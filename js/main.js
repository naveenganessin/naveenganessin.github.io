async function initialize() {
    d3.select('svg').append('text')
        .attr('x', 10)
        .attr('y', 50)
        .attr('stroke', 'Black')
        .style("font-size", 40)
        .text("Was Michael leaving the end of it?")
}
